services:
  minirouter:
    build:
      context: .
    image: minirouter:local
    ports:
      - "8080:8080"
    environment:
      MINIROUTER_CONFIG: /config/config.json
      MINIROUTER_ROUTING_DEBUG: "true"
    volumes:
      - ./config.json:/config/config.json:ro